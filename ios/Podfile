# ios/Podfile
source 'https://cdn.cocoapods.org/'

platform :ios, '13.0'
ENV['COCOAPODS_DISABLE_STATS'] = 'true'

# Locate Flutter root so CI containers (Codemagic) can load podhelper
flutter_root = ENV['FLUTTER_ROOT'] ||
               File.expand_path('../..', __dir__) + '/flutter'
require File.expand_path(
  File.join('packages', 'flutter_tools', 'bin', 'podhelper'),
  flutter_root
)

target 'Runner' do
  use_frameworks! :linkage => :static
  use_modular_headers!

  # Standard Flutter pods
  flutter_install_all_ios_pods File.dirname(File.realpath(__FILE__))
end

post_install do |installer|
  installer.pods_project.targets.each do |target|
    # Apply Flutter defaults
    flutter_additional_ios_build_settings(target)

    target.build_configurations.each do |config|
      # keep your minimum iOS version
      config.build_settings['IPHONEOS_DEPLOYMENT_TARGET'] = '13.0'

      # strip out any -G… flags (unsupported on simulators)
      if config.build_settings['OTHER_CFLAGS'].is_a?(String)
        flags = config.build_settings['OTHER_CFLAGS'].split
        filtered = flags.reject { |f| f.start_with?('-G') }
        config.build_settings['OTHER_CFLAGS'] = filtered.join(' ')
      end
    end
  end
end
