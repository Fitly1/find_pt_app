# ios/Podfile
source 'https://cdn.cocoapods.org/'

platform :ios, '13.0'
ENV['COCOAPODS_DISABLE_STATS'] = 'true'

flutter_root = ENV['FLUTTER_ROOT'] ||
  File.expand_path('../..', __dir__) + '/flutter'
require File.expand_path(
  File.join('packages', 'flutter_tools', 'bin', 'podhelper'),
  flutter_root
)

target 'Runner' do
  use_frameworks! :linkage => :static
  use_modular_headers!

  # Pin both GTMSessionFetcher/Full and /Core at 1.5.0 so FirebaseAuth’s (<5.0,>=3.4) constraint is satisfied
  pod 'GTMSessionFetcher/Full', '1.5.0'
  pod 'GTMSessionFetcher/Core', '1.5.0'

  # Then all Flutter plugins (Firebase, Stripe, etc)
  flutter_install_all_ios_pods File.dirname(File.realpath(__FILE__))
end

post_install do |installer|
  installer.pods_project.targets.each do |target|
    flutter_additional_ios_build_settings(target)
    target.build_configurations.each do |config|
      config.build_settings['IPHONEOS_DEPLOYMENT_TARGET'] = '13.0'
    end

    if target.name == 'BoringSSL-GRPC'
      target.build_configurations.each do |config|
        if (oc = config.build_settings['OTHER_CFLAGS']).is_a?(String)
          config.build_settings['OTHER_CFLAGS'] = oc
            .split
            .reject { |f| f.start_with?('-G') }
            .join(' ')
        end
      end
      target.source_build_phases&.each do |phase|
        phase.files.each do |file|
          next unless file.respond_to?(:settings)
          if (cf = file.settings['COMPILER_FLAGS']).is_a?(String)
            file.settings['COMPILER_FLAGS'] = cf
              .split
              .reject { |f| f.start_with?('-G') }
              .join(' ')
          end
        end
      end
    end
  end
end
